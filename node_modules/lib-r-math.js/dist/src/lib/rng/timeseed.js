'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var trunc = Math.trunc, ceil = Math.ceil, min = Math.min, log2 = Math.log2, pow = Math.pow;
var now = Date.now;
function timeseed() {
    var n = now();
    do {
        now();
    } while (now() - n < 500);
    var nBits = min(32, ceil(log2(n)));
    var lowBits = trunc(nBits / 2);
    var hi = trunc(n / pow(2, lowBits));
    var lo = n - hi * pow(2, lowBits);
    var buf = new ArrayBuffer(4);
    var reverser = new Uint8Array(buf);
    var uint32 = new Uint32Array(buf);
    uint32[0] = lo ^ hi;
    reverser.reverse();
    return uint32[0];
}
exports.timeseed = timeseed;
//# sourceMappingURL=timeseed.js.map