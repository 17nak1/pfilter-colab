"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var rchisq_1 = require("../chi-2/rchisq");
var _general_1 = require("../common/_general");
var printer = debug('rf');
var ISNAN = Number.isNaN, R_FINITE = Number.isFinite;
function rfOne(n1, n2, rng) {
    var v1;
    var v2;
    if (ISNAN(n1) || ISNAN(n2) || n1 <= 0 || n2 <= 0) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    v1 = R_FINITE(n1) ? rchisq_1.rchisqOne(n1, rng) / n1 : 1;
    v2 = R_FINITE(n2) ? rchisq_1.rchisqOne(n2, rng) / n2 : 1;
    return v1 / v2;
}
exports.rfOne = rfOne;
//# sourceMappingURL=rf.js.map