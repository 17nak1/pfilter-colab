'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var _general_1 = require("../common/_general");
var r_func_1 = require("../r-func");
var ISNAN = Number.isNaN, R_FINITE = Number.isFinite;
var printer = debug('rnorm');
function rnorm(n, mu, sigma, rng) {
    if (mu === void 0) { mu = 0; }
    if (sigma === void 0) { sigma = 1; }
    return r_func_1.randomGenHelper(n, rnormOne, mu, sigma, rng);
}
exports.rnorm = rnorm;
function rnormOne(mu, sigma, rng) {
    if (mu === void 0) { mu = 0; }
    if (sigma === void 0) { sigma = 1; }
    if (ISNAN(mu) || !R_FINITE(sigma) || sigma < 0) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    if (sigma === 0 || !R_FINITE(mu)) {
        return mu;
    }
    return mu + sigma * rng.norm_randOne();
}
exports.rnormOne = rnormOne;
//# sourceMappingURL=rnorm.js.map