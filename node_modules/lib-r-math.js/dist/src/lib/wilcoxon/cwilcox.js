'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var trunc = Math.trunc;
function cwilcox(k, m, n, w) {
    var i;
    var j;
    var u = m * n;
    var c = trunc(u / 2);
    if (k < 0 || k > u) {
        return 0;
    }
    if (k > c) {
        k = u - k;
    }
    if (m < n) {
        i = m;
        j = n;
    }
    else {
        i = n;
        j = m;
    }
    if (j > 0 && k < j) {
        return cwilcox(k, i, k, w);
    }
    if (w.get(i, j, k) === undefined) {
        if (j === 0) {
            w.set(i, j, k, k === 0 ? 1 : 0);
        }
        else {
            var c1 = cwilcox(k - j, i - 1, j, w);
            var c2 = cwilcox(k, i, j - 1, w);
            w.set(i, j, k, c1 + c2);
        }
    }
    var result = w.get(i, j, k);
    if (result === undefined) {
        throw new Error('WilcoxonCache not set');
    }
    return result;
}
exports.cwilcox = cwilcox;
//# sourceMappingURL=cwilcox.js.map