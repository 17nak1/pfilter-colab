'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var _general_1 = require("../common/_general");
var expm1_1 = require("../exp/expm1");
var pow = Math.pow;
var ISNAN = Number.isNaN, ML_POSINF = Number.POSITIVE_INFINITY;
var printer = debug('qweibull');
function qweibull(p, shape, scale, lowerTail, logP) {
    if (scale === void 0) { scale = 1; }
    if (lowerTail === void 0) { lowerTail = true; }
    if (logP === void 0) { logP = false; }
    if (ISNAN(p) || ISNAN(shape) || ISNAN(scale))
        return p + shape + scale;
    if (shape <= 0 || scale <= 0)
        return _general_1.ML_ERR_return_NAN(printer);
    var rc = _general_1.R_Q_P01_boundaries(lowerTail, logP, p, 0, ML_POSINF);
    if (rc !== undefined) {
        return rc;
    }
    return scale * pow(-expm1_1.R_DT_Clog(lowerTail, logP, p), 1 / shape);
}
exports.qweibull = qweibull;
//# sourceMappingURL=qweibull.js.map