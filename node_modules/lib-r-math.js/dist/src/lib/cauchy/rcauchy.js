"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var _general_1 = require("../common/_general");
var r_func_1 = require("../r-func");
var ISNAN = Number.isNaN, R_FINITE = Number.isFinite;
var M_PI = Math.PI;
var printer = debug('rcauchy');
function rcauchy(n, location, scale, rng) {
    if (location === void 0) { location = 0; }
    if (scale === void 0) { scale = 1; }
    return r_func_1.randomGenHelper(n, rcauchyOne, location, scale, rng);
}
exports.rcauchy = rcauchy;
function rcauchyOne(location, scale, rng) {
    if (location === void 0) { location = 0; }
    if (scale === void 0) { scale = 1; }
    if (ISNAN(location) || !R_FINITE(scale) || scale < 0) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    if (scale === 0 || !R_FINITE(location))
        return location;
    else
        return location + scale * Math.tan(M_PI * rng.unif_rand());
}
exports.rcauchyOne = rcauchyOne;
//# sourceMappingURL=rcauchy.js.map