'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var _general_1 = require("../common/_general");
var ISNAN = Number.isNaN;
var floor = Math.floor, round = Math.round;
var printer_rsignrank = debug('rsignrank');
function rsignrank(nn, n, rng) {
    return Array.from({ length: nn }).map(function () {
        if (ISNAN(n))
            return n;
        var nRound = round(n);
        if (nRound < 0)
            return _general_1.ML_ERR_return_NAN(printer_rsignrank);
        if (nRound === 0)
            return 0;
        var r = 0.0;
        var k = floor(nRound);
        for (var i = 0; i < k;) {
            r += ++i * floor(rng.unif_rand() + 0.5);
        }
        return r;
    });
}
exports.rsignrank = rsignrank;
//# sourceMappingURL=rsign.js.map