'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var rchisq_1 = require("../chi-2/rchisq");
var _general_1 = require("../common/_general");
var r_func_1 = require("../r-func");
var sqrt = Math.sqrt;
var ISNAN = Number.isNaN, R_FINITE = Number.isFinite;
var printer = debug('rt');
function rt(n, df, rng) {
    return r_func_1.randomGenHelper(n, rtOne, df, rng);
}
exports.rt = rt;
function rtOne(df, rng) {
    if (ISNAN(df) || df <= 0.0) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    if (!R_FINITE(df))
        return rng.norm_randOne();
    var num = rng.norm_randOne();
    return num / sqrt(rchisq_1.rchisqOne(df, rng) / df);
}
exports.rtOne = rtOne;
//# sourceMappingURL=rt.js.map