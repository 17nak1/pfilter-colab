'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var _general_1 = require("../common/_general");
var debug = require("debug");
var expm1_1 = require("../exp/expm1");
var ISNAN = Number.isNaN, R_FINITE = Number.isFinite;
var printer = debug('qunif');
function qunif(p, min, max, lowerTail, logP) {
    if (min === void 0) { min = 0; }
    if (max === void 0) { max = 1; }
    if (lowerTail === void 0) { lowerTail = true; }
    if (logP === void 0) { logP = false; }
    if (ISNAN(p) || ISNAN(min) || ISNAN(max))
        return NaN;
    var rc = _general_1.R_Q_P01_check(logP, p);
    if (rc !== undefined) {
        return rc;
    }
    if (!R_FINITE(min) || !R_FINITE(max))
        return _general_1.ML_ERR_return_NAN(printer);
    if (max < min)
        return _general_1.ML_ERR_return_NAN(printer);
    if (max === min)
        return min;
    return min + expm1_1.R_DT_qIv(lowerTail, logP, p) * (max - min);
}
exports.qunif = qunif;
//# sourceMappingURL=qunif.js.map