'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var printer = debug('punif');
var _general_1 = require("../common/_general");
var ISNAN = Number.isNaN, R_FINITE = Number.isFinite;
function punif(q, min, max, lowerTail, logP) {
    if (min === void 0) { min = 0; }
    if (max === void 0) { max = 1; }
    if (lowerTail === void 0) { lowerTail = true; }
    if (logP === void 0) { logP = false; }
    if (ISNAN(q) || ISNAN(min) || ISNAN(max)) {
        return q + min + max;
    }
    if (max < min) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    if (!R_FINITE(min) || !R_FINITE(max)) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    if (q >= max) {
        return _general_1.R_DT_1(lowerTail, logP);
    }
    if (q <= min) {
        return _general_1.R_DT_0(lowerTail, logP);
    }
    if (lowerTail) {
        return _general_1.R_D_val(logP, (q - min) / (max - min));
    }
    return _general_1.R_D_val(logP, (max - q) / (max - min));
}
exports.punif = punif;
//# sourceMappingURL=punif.js.map