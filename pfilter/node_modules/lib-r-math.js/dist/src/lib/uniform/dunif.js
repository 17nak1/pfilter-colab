'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var _general_1 = require("../common/_general");
var ISNAN = Number.isNaN;
var log = Math.log;
function dunif(x, min, max, logP) {
    if (min === void 0) { min = 0; }
    if (max === void 0) { max = 1; }
    if (logP === void 0) { logP = false; }
    if (ISNAN(x) || ISNAN(min) || ISNAN(max)) {
        return x + min + max;
    }
    if (min <= x && x <= max) {
        return logP ? -log(max - min) : 1 / (max - min);
    }
    return _general_1.R_D__0(logP);
}
exports.dunif = dunif;
//# sourceMappingURL=dunif.js.map