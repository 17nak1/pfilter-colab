"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var _general_1 = require("../common/_general");
var log = Math.log;
var ISNAN = Number.isNaN, R_FINITE = Number.isFinite;
var printer_rlogis = debug('rlogis');
function rlogis(N, location, scale, rng) {
    if (location === void 0) { location = 0; }
    if (scale === void 0) { scale = 1; }
    return Array.from({ length: N }).map(function () { return rlogisOne(location, scale, rng); });
}
exports.rlogis = rlogis;
function rlogisOne(location, scale, rng) {
    if (location === void 0) { location = 0; }
    if (scale === void 0) { scale = 1; }
    if (ISNAN(location) || !R_FINITE(scale)) {
        return _general_1.ML_ERR_return_NAN(printer_rlogis);
    }
    if (scale === 0 || !R_FINITE(location))
        return location;
    else {
        var u = rng.unif_rand();
        return location + scale * log(u / (1 - u));
    }
}
exports.rlogisOne = rlogisOne;
//# sourceMappingURL=rlogis.js.map