"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var _general_1 = require("../common/_general");
var log = Math.log, exp = Math.exp, fabs = Math.abs;
var ISNAN = Number.isNaN;
var printer_dlogis = debug('dlogis');
function dlogis(x, location, scale, give_log) {
    if (location === void 0) { location = 0; }
    if (scale === void 0) { scale = 1; }
    if (give_log === void 0) { give_log = false; }
    var e;
    var f;
    if (ISNAN(x) || ISNAN(location) || ISNAN(scale))
        return NaN;
    if (scale <= 0.0) {
        return _general_1.ML_ERR_return_NAN(printer_dlogis);
    }
    x = fabs((x - location) / scale);
    e = exp(-x);
    f = 1.0 + e;
    return give_log ? -(x + log(scale * f * f)) : e / (scale * f * f);
}
exports.dlogis = dlogis;
//# sourceMappingURL=dlogis.js.map