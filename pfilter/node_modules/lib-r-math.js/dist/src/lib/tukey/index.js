'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var ptukey_1 = require("./ptukey");
var qtukey_1 = require("./qtukey");
function Tukey() {
    function ptukey(q, nmeans, df, nranges, lowerTail, logP) {
        if (nranges === void 0) { nranges = 1; }
        if (lowerTail === void 0) { lowerTail = true; }
        if (logP === void 0) { logP = false; }
        return ptukey_1.ptukey(q, nranges, nmeans, df, lowerTail, logP);
    }
    function qtukey(q, nmeans, df, nranges, lowerTail, logP) {
        if (nranges === void 0) { nranges = 1; }
        if (lowerTail === void 0) { lowerTail = true; }
        if (logP === void 0) { logP = false; }
        return qtukey_1.qtukey(q, nranges, nmeans, df, lowerTail, logP);
    }
    return {
        ptukey: ptukey,
        qtukey: qtukey
    };
}
exports.Tukey = Tukey;
//# sourceMappingURL=index.js.map