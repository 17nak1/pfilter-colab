'use strict';
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    }
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var trunc = Math.trunc;
var frac = function (x) { return x - trunc(x); };
var fixup_1 = require("../fixup");
var irng_1 = require("../irng");
var irng_type_1 = require("../irng-type");
var timeseed_1 = require("../timeseed");
var SEED_LEN = 3;
var WichmannHill = (function (_super) {
    __extends(WichmannHill, _super);
    function WichmannHill(_seed) {
        if (_seed === void 0) { _seed = timeseed_1.timeseed(); }
        return _super.call(this, _seed) || this;
    }
    WichmannHill.prototype._setup = function () {
        this._kind = irng_type_1.IRNGType.WICHMANN_HILL;
        this._name = 'Wichmann-Hill';
        var buf = new ArrayBuffer(SEED_LEN * 4);
        this.m_seed = new Uint32Array(buf).fill(0);
    };
    WichmannHill.prototype.internal_unif_rand = function () {
        var s = this.m_seed;
        s[0] = (s[0] * 171) % 30269;
        s[1] = (s[1] * 172) % 30307;
        s[2] = (s[2] * 170) % 30323;
        var value = s[0] / 30269.0 + s[1] / 30307.0 + s[2] / 30323.0;
        return fixup_1.fixup(frac(value));
    };
    WichmannHill.prototype.fixupSeeds = function () {
        var s = this.m_seed;
        s[0] = s[0] % 30269;
        s[1] = s[1] % 30307;
        s[2] = s[2] % 30323;
        if (s[0] === 0)
            s[0] = 1;
        if (s[1] === 0)
            s[1] = 1;
        if (s[2] === 0)
            s[2] = 1;
        return;
    };
    WichmannHill.prototype.init = function (seed) {
        if (seed === void 0) { seed = timeseed_1.timeseed(); }
        var s = new Uint32Array([seed]);
        for (var j = 0; j < 50; j++) {
            s[0] = 69069 * s[0] + 1;
        }
        for (var j = 0; j < this.m_seed.length; j++) {
            s[0] = 69069 * s[0] + 1;
            this.m_seed[j] = s[0];
        }
        this.fixupSeeds();
        _super.prototype.init.call(this, seed);
    };
    Object.defineProperty(WichmannHill.prototype, "seed", {
        get: function () {
            return Array.from(this.m_seed);
        },
        set: function (_seed) {
            if (_seed.length > this.m_seed.length || _seed.length === 0) {
                this.init(timeseed_1.timeseed());
                return;
            }
            this.m_seed.set(_seed);
        },
        enumerable: true,
        configurable: true
    });
    return WichmannHill;
}(irng_1.IRNG));
exports.WichmannHill = WichmannHill;
//# sourceMappingURL=index.js.map