"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var debug = require("debug");
var _general_1 = require("../common/_general");
var ISNAN = Number.isNaN;
var M_PI = Math.PI, log = Math.log;
var printer = debug('dcauchy');
function dcauchy(x, location, scale, giveLog) {
    if (location === void 0) { location = 0; }
    if (scale === void 0) { scale = 1; }
    if (giveLog === void 0) { giveLog = false; }
    var y;
    if (ISNAN(x) || ISNAN(location) || ISNAN(scale)) {
        return x + location + scale;
    }
    if (scale <= 0) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    y = (x - location) / scale;
    return giveLog
        ? -log(M_PI * scale * (1 + y * y))
        : 1 / (M_PI * scale * (1 + y * y));
}
exports.dcauchy = dcauchy;
//# sourceMappingURL=dcauchy.js.map