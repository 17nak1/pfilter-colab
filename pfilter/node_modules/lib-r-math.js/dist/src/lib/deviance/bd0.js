"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var R_FINITE = Number.isFinite, DBL_MIN = Number.MIN_VALUE, NaN = Number.NaN;
var fabs = Math.abs, log = Math.log;
function bd0(x, np) {
    var ej;
    var s;
    var s1;
    var v;
    var j;
    if (!R_FINITE(x) || !R_FINITE(np) || np === 0.0) {
        return NaN;
    }
    if (fabs(x - np) < 0.1 * (x + np)) {
        v = (x - np) / (x + np);
        s = (x - np) * v;
        if (fabs(s) < DBL_MIN)
            return s;
        ej = 2 * x * v;
        v = v * v;
        for (j = 1; j < 1000; j++) {
            ej *= v;
            s1 = s + ej / ((j << 1) + 1);
            if (s1 === s)
                return s1;
            s = s1;
        }
    }
    return x * log(x / np) + np - x;
}
exports.bd0 = bd0;
//# sourceMappingURL=bd0.js.map