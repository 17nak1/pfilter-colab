"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var _general_1 = require("../common/_general");
var debug = require("debug");
var expm1_1 = require("./expm1");
var expm1 = Math.expm1;
var ISNAN = Number.isNaN;
var printer = debug('pexp');
function pexp(q, scale, lower_tail, log_p) {
    if (ISNAN(q) || ISNAN(scale))
        return q + scale;
    if (scale < 0) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    if (q <= 0)
        return _general_1.R_DT_0(lower_tail, log_p);
    q = -(q / scale);
    return lower_tail
        ? log_p ? expm1_1.R_Log1_Exp(q) : -expm1(q)
        : _general_1.R_D_exp(log_p, q);
}
exports.pexp = pexp;
//# sourceMappingURL=pexp.js.map