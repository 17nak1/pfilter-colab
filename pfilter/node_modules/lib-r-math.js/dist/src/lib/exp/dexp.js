"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var _general_1 = require("../common/_general");
var debug = require("debug");
var log = Math.log, exp = Math.exp;
var ISNAN = Number.isNaN;
var printer = debug('dexp');
function dexp(x, scale, give_log) {
    if (give_log === void 0) { give_log = false; }
    if (ISNAN(x) || ISNAN(scale)) {
        return NaN;
    }
    if (scale <= 0.0) {
        return _general_1.ML_ERR_return_NAN(printer);
    }
    if (x < 0) {
        return _general_1.R_D__0(give_log);
    }
    return give_log ? -x / scale - log(scale) : exp(-x / scale) / scale;
}
exports.dexp = dexp;
//# sourceMappingURL=dexp.js.map